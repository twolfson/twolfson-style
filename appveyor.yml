version: "{build}"

# environment.nodejs_version is dictated in appveyor.com; set to `0.10` and `0.12`

matrix:
  fast_finish: true

install:
  - ps: Install-Product node $env:nodejs_version
  - node --version
  - npm --version
  - npm install

test_script:
  - npm run test-windows

build: off
